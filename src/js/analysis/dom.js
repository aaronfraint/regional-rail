const button_to_select_another_destination = document.getElementById(
  "button-to-select-another-destination"
);
const button_to_make_zone = document.getElementById("button-to-make-zone");
const graph = document.getElementById("graph");

export { button_to_select_another_destination, button_to_make_zone, graph };
